{
  "name": "kitameraki-be-test",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\"",
    "db:setup": "ts-node src/scripts/initializeDb.ts",
    "db:seed": "ts-node src/scripts/seedDb.ts"
  },
  "dependencies": {
    "@azure/cosmos": "^4.0.0",
    "@azure/functions": "^4.0.0",
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1"
  },
  "devDependencies": {
    "@types/node": "^20.x",
    "rimraf": "^5.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^4.0.0"
  },
  "main": "dist/src/{index.js,functions/**/*.js}"
}
